# Pages

Route-level screens live here, grouped by domain (`auth`, `storefront`, `account`, `admin`).
Keep page files thin: compose feature hooks/components and import page-specific CSS from `src/styles/pages`.
